import * as React from 'react';
import { FC } from 'react';

interface ChatProps {
    
}
 
const Chat: FC<ChatProps> = () => {
    return (
        <h1>Welcome to chat!</h1>
    );
}
 
export default Chat;